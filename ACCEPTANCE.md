# 0.5.8 双平台验收

必须使用独立空库、虚构对话和无隐私图片。macOS 与 Windows 使用同一安装 ZIP；先核对 SHA256SUMS.txt。自动化和模拟源结果不能代替真实 Codex Desktop 验收。

以下 macOS 栏保留 0.5.1 基线结果，仅作参考，不代表 0.5.8 全部复测通过。目录、模板及 0.5.8 设置界面实测见 VALIDATION.md。

## 环境记录

平台 / 系统版本：____；Obsidian：____；Dataview：____；Codex CLI：____。
安装包 SHA256：____；测试日期：____；执行者：____。

## 操作与通过标准

| 编号 | 操作 | 通过标准 | macOS 本轮结果 | Windows 结果 |
| --- | --- | --- | --- | --- |
| A01 | 新建空库并安装 Dataview 与本插件 | 首次启用后仅提示设置入口；无任务笔记、Codex 子进程或固定起点 | 通过：独立空库首次启用无读取/起点 | 待测 |
| A02 | 未确认库外读取就检查连接；禁用 Dataview 后尝试开始 | 操作被阻止且说明原因；确认并启用后检查成功 | 通过：未确认访问被阻止；Dataview 未就绪阻止启动；启用后连接通过 | 待测 |
| A03 | 点击 Start sync，随后新建真实 Codex Desktop 主任务，发送“用两行表格和一个 JavaScript 代码块介绍虚构项目 Daybook QA”，等待正式回复 | 一个轮询周期及处理时间后出现一篇笔记；打开创建日每日记录，实际看见 Dataview 链接 | 通过：真实用户消息后生成笔记；每日链接实际显示并可打开 | 待测 |
| A04 | 在同一任务发送第二轮，附 PNG，再单独发送正式计划请求 | 同一笔记更新；用户消息、图片、表格、代码、正式计划可读；不导出工具/过程/推理 | 部分：同一笔记含多轮、PNG、表格、代码；真实协议 plan 项待补测 | 待测 |
| A05 | 连续手动同步三次 | 笔记和图片数量、内容及修改时间保持稳定 | 通过：恢复后连续三次更新/复制均为 0，修改时间不变 | 待测 |
| A06 | 同步标记外追加“我的验收补充”，增加自定义属性；继续新一轮 | 补充和自定义属性保留；已有每日笔记字节不变 | 通过：第四轮写入后个人文字/属性保留，每日文件字节不变 | 待测 |
| A07 | 备份后临时移走测试图片原件，再更新同一任务 | 已保存副本仍显示，未重复复制；首次缺失图片有提示且不丢正文 | 自动回归通过；实机缺失原件/首次缺失场景待测 | 待测 |
| A08 | 暂停、禁用、重启；在关闭期间创建测试任务，恢复 | 起点和旧路径不变；关闭期间新建任务会被发现；原 Codex 应用不被关闭 | 部分：暂停、重开库、恢复后起点映射不变；关闭期间新任务/卸载待补测 | 待测 |
| A09 | 修改新任务目录/时区，再建任务；使用同名或大小写不同标题 | 旧任务输出位置不变；新任务使用新设置；文件不覆盖 | 自动回归通过；实机待测 | 待测 |
| A10 | 测试任务归档、来源不可用、连接中断后重连 | 本地副本保留；错误不宣称任务已删除；重连后继续 | 自动回归通过；实机待测 | 待测 |
| A11 | 深浅色、阅读模式、实时预览、窄窗口；点击横/竖图片 | 等比预览、原图新标签、排版正常 | 部分：深浅色、阅读/实时预览、PNG 新标签通过；横竖多图/窄窗待补测 | 待测 |
| A12 | 在测试库使用已备份的旧版配置升级 | 起点、任务/图片映射、个人内容保留；旧版任务完整核对一次后无变化零写入 | 旧状态迁移自动回归通过；实际旧版升级待测 | 待测 |
| A13 | 运行自动故障注入测试 | 写入失败、最终状态失败、暂停/卸载中断均能恢复，无重复笔记/图片 | 通过：自动故障回归；另在真实库注入一次正文失败并成功恢复 | 待测 |

旧任务、普通 ChatGPT、子代理、未完成轮次的排除在自动测试中验证；真实运行中一并检查没有误导入。跨午夜和夏令时使用自动化固定时钟测试，实际时区与日期再做人工核对。

## 登录专项

- [ ] 未确认访问前点击登录不启动进程；已登录账号直接复用。
- [ ] 在未登录的独立测试环境中，打开官方网页、手动完成登录，插件自动报告成功。
- [ ] 取消/浏览器失败/进程关闭/禁用后可重试，不退出已有 Codex 账号；不保存授权 URL、密码或令牌。
- [ ] 自动检测失败时能在高级设置中选择程序；Windows 验证真实安装路径。

## 双语专项

- [ ] 中文 / 英语可切换；保存后设置、命令、状态、校验提示与复制反馈语言一致。
- [ ] 重启后保留所选语言；首次默认遵循 Obsidian 语言；目录、起点、映射及已有笔记不变。

## 反馈格式

编号、通过/失败、软件版本、复现步骤、预期/实际结果、脱敏截图。不要上传 data.json、登录文件、真实聊天、完整个人路径。

任一数据丢失、覆盖、误导入、重复写入或生命周期故障均阻塞发布。Windows 专项继续执行 WINDOWS-CHECKLIST.md。所有待测项明确保留为待测，禁止由单元测试替代勾选。
