# 每日轨迹开关与设置面板重构 — 实机测试任务单

状态：**未验证**。本文覆盖的是这一轮改动（每日轨迹开关、设置面板改为两个标签页、Codex 可执行文件挪位、时区可选、语言即时切换、按钮排版）——自动化测试（104 项通过）和 ESLint 发布检查已经跑过，但没有人在真实 Obsidian 里点过一遍，这份清单就是补这一步。

请使用独立的空测试库、虚构任务和无隐私图片，不要在真实工作库上做这轮测试。记录 macOS 版本、Obsidian 版本、Dataview 版本；不要提交真实聊天内容、`data.json` 或带个人路径的截图。

## 0. 准备

1. 新建一个空 Obsidian 库（或用已有的干净测试库），安装并启用 Dataview。
2. 从 `dist/` 或最新打包结果安装插件到该测试库的 `.obsidian/plugins/codex-daily-sync/`，重载 Obsidian 后启用插件。
3. 打开插件设置页，准备开始。

## 1. 全新安装的默认状态

- [ ] 首次打开设置页，顶部是两个标签按钮："基础设置"（高亮/选中态）和"每日轨迹"（未选中）。
- [ ] 默认停在"基础设置"标签，"每日轨迹"标签内容不可见。
- [ ] 切到"每日轨迹"标签：能看到"启用每日轨迹"开关，且默认是**关闭**的；开关下方的"每日笔记保存位置""使用自己的每日笔记模板"等字段应该是隐藏/不可见的。
- [ ] 打开"启用每日轨迹"开关：下方字段立即出现（不用保存也能看到），"每日笔记保存位置"默认显示 `Daily`，模板字段默认为空——说明开关本身就是"一键开启"，不需要再手动填什么。

## 2. 标签页切换

- [ ] 点击"每日轨迹"标签：面板切换，"基础设置"标签的内容（语言、目录、执行文件等）应该消失，"每日轨迹"标签的按钮变成高亮态。
- [ ] 点击"基础设置"标签：切回去，内容和高亮态都正确复原。
- [ ] 在"每日轨迹"标签里往"每日笔记保存位置"输入框里打几个字（先不保存），切到"基础设置"标签，再切回"每日轨迹"标签——刚才打的字应该还在，没有被清空。

## 3. 基础设置标签页逐项检查

- [ ] "语言"下拉框：选择"简体中文"或 English，**不点保存**，整个设置面板应该立即变成对应语言（所有标题、说明文字、按钮文字都变了）。
- [ ] 语言切换后，检查之前在别的字段里打的未保存内容（比如笔记目录）**没有丢失**。
- [ ] 语言切换后，如果当前在"每日轨迹"标签，切换完应该还停留在"每日轨迹"标签，不会跳回"基础设置"。
- [ ] "对话笔记目录""附件目录"：输入部分文字，应该弹出库内已有目录的提示列表；也允许直接填一个新目录名。
- [ ] "Codex 程序路径"字段：**在基础设置标签页最上面**（和笔记/附件目录并列），不在任何折叠区域里。
- [ ] 点击"一键扫描"：如果本机能找到 Codex，字段自动填入路径，下方状态栏显示"已找到 Codex：xxx"。
- [ ] 点击"浏览…"：弹出文件选择器，选中一个文件后路径回填到输入框。
- [ ] "时区"字段：开始输入（比如"Asia/Sh"），应该弹出可选时区的下拉提示；也可以直接手动填完整的 IANA 名称。
- [ ] "检查间隔（秒）"：填入 5–3600 之外的数字（比如 2），点"保存设置"应该报错提示"检查间隔必须是 5–3600 之间的整数秒"。
- [ ] "保存设置 / 检查连接"这一行现在应该有标题"保存并验证"和说明文字，不再是左边空白、按钮挤在右边。
- [ ] "开始同步 / 暂停同步"这一行应该有标题"同步控制"和说明文字；"开始同步"按钮应该是**高亮/强调色**，和"暂停同步"视觉上不一样。
- [ ] "复制查询代码"这一行（在"每日轨迹"标签里）也应该有标题和说明文字，不是空的。

## 4. 每日轨迹功能的真实同步行为（这部分最重要，自动化测试只能模拟，不能替代）

以下操作在**开启每日轨迹**的情况下进行：

- [ ] 保存设置、检查连接通过后点击"开始同步"。在 Codex Desktop 里完成一轮新对话。等一个同步间隔后，检查：
  - 生成的对话笔记 frontmatter 里有 `daily: "[[Daily/日期]]"` 字段。
  - `Daily/日期.md` 这篇笔记被自动创建，内容包含 Dataview 查询代码块。
  - 打开这篇每日笔记（需要 Dataview 已启用），能看到指向刚才那篇对话笔记的可点击链接。
- [ ] 禁用 Dataview 插件后点击"检查连接"或"开始同步"：应该被阻止，提示"请先安装并启用 Dataview"。重新启用 Dataview 后恢复正常。

以下操作切换到**关闭每日轨迹**：

- [ ] 关闭"启用每日轨迹"开关，保存设置。禁用 Dataview 插件（确认它不再是必需的）：这时应该能正常"检查连接"和"开始同步"，不再要求 Dataview。
- [ ] 完成一轮新的 Codex 对话，同步后检查：新生成的笔记 frontmatter 里**没有** `daily` 字段；`Daily/` 目录下**没有**新增当天的每日笔记文件。
- [ ] 如果之前（第 4 节第一部分）已经生成过带 `daily` 字段的旧笔记，现在这篇任务再触发一次更新（比如在 Codex 里继续对话），检查旧笔记里原来的 `daily` 字段**依然保留**，没有被这次"关闭状态"的同步删掉。

## 5. 升级场景（模拟已有用户）

- [ ] 找一份这次改动之前版本的 `data.json`（或者手动构造一份 `schemaVersion: 3` 且 `settings` 里没有 `dailyTrackEnabled` 字段的配置），用它启动插件。
- [ ] 打开设置页确认"每日轨迹"开关是**开启**状态（老用户升级后不应该突然被关掉，导致每日链接断掉）。

## 反馈格式

每一项写清楚：测试项、通过/失败、Obsidian 和 Dataview 版本、失败时的复现步骤和脱敏截图（不要带真实任务内容或个人路径）。测试没做完或有失败项之前，不要认为这轮改动"验收通过"。
