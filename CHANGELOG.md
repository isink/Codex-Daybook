# 0.5.9 — Windows 商店版 Codex 检测

- Windows：支持从 Microsoft Store 安装的 Codex Desktop。“一键扫描”和自动检测会找到商店版自带的 `codex.exe`（位于受保护、带版本号的 WindowsApps 目录），商店更新后原先保存的路径失效时也会自动找到新位置。

# 0.5.8 — 每日轨迹开关、设置面板重构与图片同步

- 新增可关闭的"每日轨迹"功能：新装默认关闭，已有用户升级后自动保持开启，不影响历史每日链接；关闭时同步不再写入 `daily` 字段、不创建每日笔记，但仍保留笔记中已有的每日字段。
- Dataview 改为仅在开启每日轨迹时才作为同步前置条件；仅收录对话不再强制要求安装 Dataview。
- 设置面板改为"基础设置"/"每日轨迹"两个标签页，去掉原先折叠的"高级设置"区块。
- Codex 可执行文件路径移到基础设置最上层，与笔记/附件目录并列；时区字段改为可从系统时区列表中选择，也可直接填写 IANA 名称。
- 切换界面语言时设置面板立即以新语言重新渲染，不需要先点保存；未保存的其他字段与当前所在标签页不受影响，刷新方式改为官方 `this.update()` 接口，符合 Obsidian 1.13+ 设置面板规范。
- 保存/检查连接、开始/暂停同步、复制查询代码几行补上标题和说明文字；"开始同步"改为高亮的主操作按钮。
- 每日笔记内置模板的 `type` 字段改为中文"每日记录"。
- Codex 用内置图像工具生成的图片现在会复制进附件目录，并显示在该轮回答下方；Codex 自己的缓存文件被清理时改用对话记录中的图片数据。已同步过的任务升级后会自动完整核对一次，补上以前漏掉的生成图片。
- 附件目录改为按对话命名（与对话笔记同名，同名冲突时加任务短码），首次收录时确定，之后对话改名也不变；已有按任务 ID 命名的附件目录升级后自动整体改名，笔记里的图片链接（包括 Codex 自己写的链接）同步更新，原件已不存在的图片也不会丢。
- 修复：升级后的一次性完整核对会把用户已删除的对话笔记重新创建出来。现在删除的笔记保持删除，只有该对话出现新消息时才会重建（与之前一致）。

# 0.5.7 — 社区目录审核清理

- 在根 README 增加英文摘要，便于社区目录直接展示与审核。
- 用更高选择器特异性替换对话图片规则中的 `!important`，保持样式仅作用于 Codex Daybook 对话卡片。

# 0.5.6 — 每日模板选择

- 每日模板字段提示库内 Markdown 文件，并提供“选择模板…”搜索入口。
- 模板只接受 Markdown 文件；目录字段、同步规则和已有配置不变。

# 0.5.5 — 目录补全

- 对话笔记、附件与每日笔记目录在输入时提示当前库内已有目录。
- 目录仍可手动填写，允许同步时创建新的库内路径；每日模板、同步规则和已有配置不变。

# 0.5.4 — 每日笔记设置说明

- 中英文说明每日笔记的用途、库内文件夹填写示例、模板留空行为与自定义模板路径。
- 解释对话列表如何显示，以及自定义模板的日期占位符限制。
- 仅调整界面说明；同步规则、默认路径和现有配置保持不变。

# 0.5.3 — 插件内登录 Codex

- 新增中英双语登录 / 取消登录按钮，自动检测本机程序；已有账号直接复用，未登录时打开官方 ChatGPT 授权网页。
- 可执行文件选择器收进默认折叠的高级设置。
- 验证登录完成通知并重新核对账号；取消和禁用清理本次进程，不退出共享账号，不自动开始同步。
- 更新隐私与验收说明，插件不保存密码、账号令牌或授权 URL。

# 0.5.2 — 中文与英语界面

- 增加简体中文与 English 两种界面语言，首次按 Obsidian 语言选择，保存后记住选择。
- 设置、说明、状态栏、命令、连接与校验提示采用所选语言。保存后立即刷新，保留目录、同步起点与任务映射。
- 仅更改界面语言，不翻译对话原文或重写已有笔记。旧版没有语言字段的配置自动补齐，不重置同步起点。
- 0.5.1 的验收证据保留在对应版本发布目录；0.5.2 需单独完成剩余上架验收。

# 0.5.1 上架前候选版

- 修复提前保存轮次签名导致笔记写入失败后跳过重试的问题；附件缓存与发布状态分开提交。
- 为旧版任务做一次完整核对，保留起点、固定路由和个人文字；首次索引失败可恢复。
- 新增写入/状态保存失败、图片恢复、停止中断、旧版核对和发布包隐私回归测试。
- 修复文件选择器取消残留、复制查询反馈和两处界面内联样式。
- 统一 0.5.1 版本来源；采用 MIT，将项目和 yaml 许可嵌入 main.js。
- 增加官方 ESLint、发布检查、macOS/Windows CI、双平台验收表和独立 Release 文件。
- Windows 实机验收、远端 CI、GitHub 发布和社区审核仍待完成；详见 VALIDATION.md。

# 0.5.0 本地候选版

- 新增首次设置、访问确认、连接检查、开始/暂停和重连。
- 通用目录、模板、时区、间隔；固定任务级创建日与输出位置。
- Windows 可执行文件/路径、保留文件名、大小写冲突与 CRLF 处理。
- 脱敏源码、双语说明、依赖声明和白名单打包。
- 将 yaml 升级到 2.8.3，修复深层嵌套解析问题（[维护者公告](https://github.com/eemeli/yaml/security/advisories/GHSA-48c2-rrv3-qjmp)）。
- 保留旧版过滤、问答气泡、小图、原图映射、每日轨迹和本地保留规则。
- 修复首次从暂存文件发布笔记时没有立即触发 Obsidian 索引的问题，保证每日 Dataview 轨迹收录；重复同步仍零写入。
- 来源不可用且库内笔记缺失时明确区分，不再把缺失文件计入“已有笔记”数量。
- Windows 待实机验证；许可证待确定，未公开发布。

## 回滚 / Rollback

1. 先暂停并禁用插件，备份当前整个插件目录、笔记和附件。
2. 恢复升级前备份里的 `main.js`、`manifest.json`、`styles.css` 和 **对应版本的 `data.json`**。不要让 0.4 读取 0.5 的 schemaVersion 3 配置。
3. 启用恢复的插件，检查起点和笔记。不要删除新生成的笔记或图片来“修复”版本差异；先备份并人工核对。

Downgrade requires the matching old configuration as well as old binaries. Keep newer notes/assets separately; do not automatically delete them.

## 0.5.0 历史上架门槛（当时状态）

- [ ] 用户决定项目许可证并补充 LICENSE，复核所有依赖声明。
- [ ] Windows 实机清单通过，失败项修复并回归。
- [ ] 检查 Obsidian 最低版本及最新审核政策，完成兼容性验证。
- [ ] 用户明确授权创建公开 GitHub 仓库及上传内容，再进行隐私审查。
- [ ] 以版本号对应的 tag 创建 release，提供官方要求的插件文件。
- [ ] 用户另行授权社区提交；提交并不等于审核通过。

本轮以上公开操作均未执行。
