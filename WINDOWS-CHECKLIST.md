# Windows 实机验收 / Hardware validation pending

状态：**未验证**。macOS 上的路径单元测试不能代替 Windows 实机测试。同一安装 ZIP 供两平台使用。

## 0.5.10 测试包安装

1. 将 `codex-daily-sync-0.5.10.zip`、`SHA256SUMS.txt`、本文及 `ACCEPTANCE.md` 复制到 Windows。
2. 在 PowerShell 执行 `Get-FileHash .\codex-daily-sync-0.5.10.zip -Algorithm SHA256`，与校验文件中同名安装包的值核对。两平台使用相同 ZIP。
3. 创建独立空库并安装 Dataview；将 ZIP 内的 `codex-daily-sync` 文件夹放进测试库 `.obsidian/plugins/`，重载 Obsidian 后启用插件。
4. 以 Obsidian **1.13.7** 为最低版本基线，并记录 Dataview 与 Codex CLI 版本；按以下项目及 ACCEPTANCE.md 逐项填写。

请使用独立空库、虚构任务与无隐私图片。记录 Windows、Obsidian、Dataview、Codex CLI 版本；不要提交真实聊天、`data.json` 或带私人路径的截图。

- [ ] 解压安装后能启用；设置页打开，无笔记、任务读取或子进程自动出现。
- [ ] 未确认库外访问或未检查连接时，“开始同步”被阻止。
- [ ] Dataview 禁用时不能开始；启用后恢复。
- [ ] 从带空格/中文的安装路径选择真实 `codex.exe`，登录与分页检查通过。PATH 检测也测试一次。`.cmd`、不存在路径和 WSL 路径有明确失败提示。
- [ ] 点击开始后新建一个 Codex Desktop 主任务，完成一轮；一个间隔后只生成一篇笔记及创建日每日文件。旧任务、普通 ChatGPT 和子代理不导入。
- [ ] 第二轮更新同一文件，无重复 YAML、每日条目或附件。长文本、表格、代码和正式计划可读。
- [ ] drive 路径、中文文件名、UNC 共享目录中的显式 PNG/JPEG 附件可读取（先确认共享本身可访问）。CRLF 包装清理正确，真实路径/英文/代码保留。
- [ ] 横图/竖图/多图等比最大 240×180，点击新标签打开原图；失效原图仍能复用副本。首次缺失显示提示，不丢文字。
- [ ] `CON`、`NUL.txt` 等标题生成安全文件名；`Example` 和 `example` 不覆盖。
- [ ] 缺失每日记录用默认 Dataview 模板创建；已有每日笔记字节不变。自定义日期模板正确，不运行脚本。
- [ ] 暂停、重启 Obsidian、重连后起点不变；关闭期间新建的任务恢复时仍被发现。
- [ ] 修改目录或时区后旧任务路径/每日链接不变，新任务使用新设置。测试跨午夜与夏令时日期。
- [ ] 归档、来源不可读或连接退出后保留本地笔记和图片；错误不宣称来源已删除。检查连接失败时无新文件。
- [ ] 禁用/卸载后轮询停止，只结束插件自己的 app-server；原 Codex 应用继续正常运行。
- [ ] 阅读模式、实时预览、深浅色主题及窄窗排版正常。
- [ ] 重复同步和重载后，笔记及附件数量稳定，无非预期外部网络请求。

反馈格式：测试项、通过/失败、软件版本、可复现步骤、脱敏错误截图。失败后先暂停，不要删除配置。测试结果返回前，不标记 Windows 已支持/已验收。
