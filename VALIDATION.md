# 0.5.8 每日轨迹开关、设置面板重构与图片同步验证（2026-09-24）

新增可关闭的"每日轨迹"功能，设置面板改为"基础设置/每日轨迹"两个标签页；增加 Codex 生成图片导入与按对话命名附件目录，并修复升级核对重建已删除笔记的问题。

- 0.5.7 的社区审核清理已经发布；0.5.8 使用新标签和新 Release。0.5.6 与更早版本的历史验收保留在下文。
- 每日轨迹开关：新装默认关闭；schemaVersion 3 配置缺失该字段时按已有用户对待、自动回填为开启，不影响历史每日链接；关闭时不写入 `daily` 字段、不创建每日笔记，但仍保留笔记中已有的 `daily`/`created_at`/`captured_at` 字段。Dataview 改为仅在开启每日轨迹时才作为同步前置条件。
- 设置面板改为"基础设置"/"每日轨迹"两个标签，去掉原先折叠的"高级设置"；Codex 可执行文件、时区、检查间隔移入基础设置标签页，时区支持从系统时区列表选择。
- 切换界面语言立即以新语言重绘设置面板，不需要先保存；未保存的其他字段与当前标签页不受影响；面板刷新改为官方 `this.update()` 接口，符合 Obsidian 1.13+ 设置面板 lint 规范。
- Codex 生成的图片：此前只导入用户消息里明确附带的本地图片，Codex 图像工具生成的图片被静默丢弃。现在读取已完成轮次中 `imageGeneration` 项的本地保存路径（缺失时改用其内联图片数据），复制进附件目录并显示在该轮回答下方；失败或未完成的生成不导入。已同步任务升级后完整核对一次以补图。
- 附件目录按对话命名：首次收录时取对话笔记文件名（尚无笔记时取对话标题），同名冲突加任务短码，之后不随改名变化；旧的任务 ID 目录在升级核对中整体改名并更新笔记链接，原件已删除的图片随目录一起保留。
- 修复一次性完整核对会重建用户已删除笔记的问题（此前升级后曾在实机库重建过一篇已删除的对话笔记）：笔记已删除且对话无新消息时保持删除；用户移动过的笔记仍按任务 ID 找到并原地更新。
- 自动测试 114 项通过（较上一版本新增 20 项，覆盖每日轨迹开关默认值/迁移、标签页切换、语言即时预览、Dataview 条件门槛、生成图片导入/回退/忽略失败、升级后一次性补核对、附件目录命名/冲突/旧目录迁移、已删除笔记不重建等场景）；官方 ESLint 0 错误，保留 1 条既有全局设置搜索建议。构建、版本、许可、白名单及隐私扫描通过。
- macOS / Obsidian 1.13.7 独立测试库已加载本轮构建：全新设置默认值、每日轨迹开关与标签切换、未保存输入保留、中英文即时切换、目录/模板/时区候选、一键扫描、文件选择器取消、非法间隔提示、按钮说明与主操作样式均通过实际点击检查。内容同步、升级迁移与 Windows 完整矩阵仍按 ACCEPTANCE.md 保留待测项，不能由自动化结果替代。
- 当前社区条目已公开，0.5.7 已从社区渠道可安装；条目尚未经过 Obsidian 员工人工审核。Windows 实机与完整双平台验收仍待完成。

---

# 0.5.6 每日模板选择候选版验证

仅增加每日模板文件补全与搜索入口；同步逻辑、默认值与配置结构不变。

- 自动测试 94 项通过；官方 ESLint 0 错误，保留 1 条既有全局设置搜索建议。构建、版本、许可与隐私扫描通过。
- macOS / Obsidian 1.13.7 已从本版候选安装文件实际加载 0.5.6。中文设置页的每日模板字段显示为可输入的下拉框，并提供“选择模板…”按钮。
- 实际点击“选择模板…”后，插件列出当前库可用的 Markdown 文件；选中一项会回填模板路径。测试选择已清空，三个未保存的目录草稿已恢复，未擅自保存或改变原有配置。
- 更新前后已核对插件 `data.json` 的 SHA256 一致；同步保持暂停，未读取任务、未新建笔记或附件。
- 本轮仅验收模板选择，未重做真实同步、浏览器登录或完整双平台验收；Windows 与原发布门槛仍待完成。源码仓库已公开；GitHub Release、社区提交和社区安装结果另行记录，不以源码公开替代验收。

---

## 0.5.5 目录补全候选版验证

仅增加设置页目录补全；同步逻辑、默认值与配置结构不变。三个目录字段显示库内已有目录，仍允许填写新的库内相对路径。

- 自动测试 93 项通过；官方 ESLint 0 错误，保留 1 条既有全局设置搜索建议。构建、版本、许可与隐私扫描通过。
- macOS / Obsidian 1.13.7 已从本版候选安装文件实际加载 0.5.5。中文设置页的对话笔记、附件和每日笔记目录均显示为可输入的下拉框；输入部分路径会弹出匹配的库内已有目录。
- 更新前后已核对插件 `data.json` 的 SHA256 一致；同步保持暂停，未读取任务、未新建笔记或附件。更新造成的未保存目录草稿已恢复，未擅自保存或改变原有配置。
- 本轮仅验收目录补全，未重做真实同步、浏览器登录或完整双平台验收；Windows 与原发布门槛仍待完成，未公开发布。

---

## 0.5.4 设置说明候选版验证

仅调整中英文设置说明；同步逻辑、默认值与配置结构不变。历史候选包保留。

- 自动测试 92 项通过；官方 ESLint 0 错误，保留 1 条既有全局设置搜索建议。构建、版本、许可与隐私扫描通过。
- macOS / Obsidian 1.13.7 已实际加载 0.5.4，设置页显示新的中文标签、填写示例、留空行为及对话列表说明；截图核对无截断或遮挡。
- 升级前备份插件；安装未改写 data.json。加载后核对原配置、固定起点及映射，自动同步状态保留。
- 英文文案与翻译占位符通过自动检查；本轮未在桌面切换英语，也未重做完整同步或双平台验收。
- Windows、未登录账号的首次浏览器授权及原发布门槛仍待完成；未公开发布。

---

## 0.5.3 历史登录候选版验证

**已实现插件内官方浏览器登录入口，尚未上架。** 保留 0.5.1 / 0.5.2 原候选包，0.5.3 使用新的包与校验值。

- 官方接口：核对 OpenAI App Server 文档，并用本机 CLI 生成的协议 Schema 确认 account/login/start、account/login/completed、account/login/cancel 与 account/read。仅采用 Codex 托管 ChatGPT 登录，不接入外部令牌模式。
- 自动测试：92 项通过，0 失败。新增 URL 校验、登录完成与账号复核、提前/重复/无关通知、取消、浏览器失败、进程关闭、卸载中断、RPC 通知传递、访问确认门槛及已有账号复用覆盖。
- macOS 实机：Obsidian 1.13.7 的独立测试库已加载 0.5.3。中文“登录 Codex / 取消登录”按钮可见，程序路径在默认折叠的高级设置内。实际点击登录后自动找到本机 Codex，显示“已登录 Codex”。原账号不需重复授权，未开始同步。
- 数据保护：实机登录检查前后配置完全一致，已有 Markdown 文件逐字节不变。密码、令牌、授权 URL 与临时登录 ID 不写入插件 data.json 或发布文件。
- 官方 ESLint 0 错误，保留 1 条既有全局设置搜索建议；版本、许可与隐私扫描通过。独立临时目录从源码包安装依赖、检查和打包通过，全部产物与本地候选逐字节一致，SHA256 校验通过。
- 未登录账号的真实浏览器授权、Windows 实机与原双平台剩余项目仍待验收。自动回归不替代用户在官方网页完成登录的实机证据。
- 未调用本地大模型；未创建远端发布或提交社区目录。

以下为历史基线记录，不代表 0.5.3 全部重新完成了实机验收。

---

## 0.5.2 历史双语候选版验证

**中文 / 英语界面已实现并完成 macOS 专项实测；尚未上架。** 0.5.1 原候选包保留，本版使用新的版本号、安装包和校验值。

- 自动测试：80 项通过，0 失败；新增语言默认值、旧配置补齐、保存/重载/切回英语、保存失败保护及中文校验提示覆盖。
- 官方 ESLint：0 错误，保留 1 条既有“设置全局搜索”建议；版本、打包许可、源码白名单及隐私模式检查通过。
- macOS Obsidian 1.13.7 独立测试库：安装 0.5.2，实际执行英语 → 简体中文 → 英语 → 简体中文。每次保存后设置页立即刷新，中文命令面板、状态栏和复制查询反馈均实际显示。
- 重载测试库后仍显示简体中文；落盘配置与升级前相比仅增加语言字段，起点、目录、任务/附件映射和访问确认保持一致。未因切换语言重写已有笔记。
- 干净源码重建：在独立临时目录安装锁定依赖，测试、构建和打包通过；全部发布文件及两个 ZIP 与工作目录产物逐字节一致，SHA256SUMS.txt 校验通过。
- 未调用本地大模型。

Windows 语言界面与完整双平台验收仍待完成；远端 CI、公开仓库/Release、社区审核与社区渠道安装仍未执行。下面记录的是 **0.5.1 历史基线**，不得直接视为 0.5.2 全部实机复测结果；双语专项及待测项见 ACCEPTANCE.md。

---

## 0.5.1 历史验证状态（2026-09-19）

**本地候选包；核心故障已关闭，尚未满足双平台正式上架门槛。** 本文仅保存测试汇总，不包含真实任务 ID、个人路径或聊天截图。

## 本轮已实际执行

- 自动测试：75 项通过，0 失败。新增 6 项同步故障恢复测试与 2 项分发/隐私扫描测试；原有界面测试改为验证取消清理和复制成功反馈。
- 官方 eslint-plugin-obsidianmd 0.4.2 推荐检查：0 错误，1 条建议。建议为传统设置面板尚未接入 getSettingDefinitions 全局设置搜索；保留原界面，不屏蔽该规则。CommonJS、路径控制字符拒绝表达式有明确的语言配置说明。
- 真实 Codex App Server：CLI 0.155.0-alpha.9.2 登录、来源字段及轮次/消息分页连接检查通过。此前 0.154.0-alpha.6.2 是历史验证记录，不等于所有中间版本都兼容。
- macOS 26.5.1 独立空库：Obsidian 1.13.7、Dataview 0.5.68，加载 0.5.1；首次启用无来源读取/子进程/固定起点，未确认访问时连接被阻止，确认后真实连接检查通过，Start sync 保存起点。
- 真实主任务：用户在 Codex Desktop 手动发送第三轮消息和无隐私 PNG 后，来源发现成功；前三轮回复、表格、代码和图片出现在同一篇笔记。工具创建但没有手动用户消息的任务在当前 CLI 的 thread/list 中未出现，此限制如实保留。
- 每日轨迹：实际每日页面渲染时间与任务链接，点击进入上述笔记；并非只检查查询文本。
- 故障恢复：在真实 Obsidian 测试库对目标笔记注入一次写入失败；插件报告错误并保留待同步状态；恢复后第四轮回复写入，自定义属性及个人补充仍在，已有每日文件字节不变。
- 幂等：恢复后连续同步三次，更新笔记 0、复制图片 0，笔记修改时间未变；重开测试库、检查连接和恢复同步后起点及任务映射保持。最终测试库已暂停。
- 显示：实际检查深浅色、阅读模式和实时预览；点击所同步 PNG 在新标签显示原图。横竖多图和更窄窗口的完整矩阵仍待补测。
- 构建与分发：从独立临时目录的源码包安装锁定依赖，测试、构建、打包通过；重建的运行文件与候选包逐字节一致。依赖审计无已知漏洞。版本、源码白名单、私人路径/任务标识/凭据扫描通过；最终校验值见 SHA256SUMS.txt。
- MIT LICENSE 已加入，main.js 包含项目及 yaml 运行时完整许可。独立声明仍保留。
- 本地 Git 仓库已初始化；尚无远端、公开提交或社区提交。
- 当日检查[官方社区目录](https://github.com/obsidianmd/obsidian-releases/blob/master/community-plugins.json)：未发现相同 ID 或名称的已收录条目；不排除审核中的申请，提交前仍须复查。

## 待完成 / 发布阻塞

- macOS 补充桌面场景：真实协议 plan 项、首次图片缺失后补齐、关闭期间新建任务、改目录/时区与标题冲突、归档/连接中断、旧版配置实际升级、横竖多图及窄窗口。相关自动测试已通过，不据此勾选实机完成。逐项见 ACCEPTANCE.md。
- Windows 实机：由用户执行 WINDOWS-CHECKLIST.md 与 ACCEPTANCE.md，待结果。
- GitHub Actions 矩阵：配置已提供，尚未在远端运行；不能称 Windows CI 已通过。
- 提交前再次核对名称/ID；仓库发布、正式 Release、自动审核和社区安装尚未执行。

## 结论边界

单元测试、模拟源、连接成功、看到设置页面、创建工具任务均不能替代真实新任务生成笔记与每日轨迹。Windows 通过前不宣传双平台已验收；社区审核并实际安装通过前不称已上架。
